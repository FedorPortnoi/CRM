CONTEXT

Project root: /c/Users/fedor/crm
React Native + Expo SDK 51 + TypeScript.
File to modify: src/app/(tabs)/contacts.tsx

The file currently has NO RefreshControl and NO isRefreshing state.
It uses a FlatList as the main scrollable component.
fetchContacts(pageNum, reset) is the data loading function.


TASK

Add pull-to-refresh to src/app/(tabs)/contacts.tsx.

Make exactly these 4 changes and nothing else:

1. Add RefreshControl to the react-native import.
   Current import line:
     import { View, Text, FlatList, TextInput, TouchableOpacity, StyleSheet, ActivityIndicator, ListRenderItemInfo, Alert, Modal } from 'react-native';
   New import line:
     import { View, Text, FlatList, TextInput, TouchableOpacity, StyleSheet, ActivityIndicator, ListRenderItemInfo, Alert, Modal, RefreshControl } from 'react-native';

2. Add isRefreshing state after the existing isAssigning state declaration:
     const [isRefreshing, setIsRefreshing] = useState<boolean>(false);

3. Add handleRefresh callback. Place it after handleRetry (around line 113) and before handleLongPressContact:
     const handleRefresh = useCallback((): void => {
       setIsRefreshing(true);
       setPage(1);
       setSelectedContactIds([]);
       void fetchContacts(1, true).finally(() => setIsRefreshing(false));
     }, [fetchContacts]);

4. Add refreshControl prop to the FlatList (the main FlatList with data={filtered}, NOT the inner modal FlatList):
     refreshControl={
       <RefreshControl
         refreshing={isRefreshing}
         onRefresh={handleRefresh}
         colors={['#1A73E8']}
         tintColor="#1A73E8"
       />
     }

Do NOT change anything else. All existing logic, styles, state, callbacks, and JSX must remain identical.
Write the complete modified file to src/app/(tabs)/contacts.tsx.


CONSTRAINTS

1. TypeScript only — zero any.
2. No comments.
3. Keep ALL existing logic, styles, and JSX unchanged except the four changes listed above.
4. The finally in handleRefresh must call setIsRefreshing(false) so the spinner stops even if fetchContacts throws.
5. handleRefresh must clear selectedContactIds on refresh.
6. Only add refreshControl to the outer FlatList (data={filtered}), not the inner one inside the Modal.


OUTPUT

Write the complete file: src/app/(tabs)/contacts.tsx


VALIDATION

After writing the file:
1. Run: npx tsc --noEmit
2. Report any TypeScript errors and fix all of them before finishing.
