CONTEXT

Project root: /c/Users/fedor/crm
React Native + Expo SDK 51 app with TypeScript.
@tanstack/react-query v5 is ALREADY installed.
These packages are ALSO just installed and available:
  @tanstack/react-query-persist-client  (v5)
  @tanstack/query-async-storage-persister (v5)
  @react-native-async-storage/async-storage (v3)

REACT QUERY V5 API NOTES (CRITICAL — v5 has breaking changes from v4):
  - "cacheTime" was renamed to "gcTime" in v5. Use gcTime not cacheTime.
  - QueryClient is imported from '@tanstack/react-query'
  - PersistQueryClientProvider is imported from '@tanstack/react-query-persist-client'
  - createAsyncStoragePersister is imported from '@tanstack/query-async-storage-persister'
  - AsyncStorage is imported from '@react-native-async-storage/async-storage'
  - PersistQueryClientProvider accepts: client={queryClient}, persistOptions={{ persister }}
  - Do NOT use QueryClientProvider from react-query — use PersistQueryClientProvider instead

Here is the CURRENT EXACT content of src/app/_layout.tsx (read it carefully):

```typescript
import React, { useEffect, useRef, useState } from 'react';
import { ActivityIndicator, View } from 'react-native';
import { Stack, useRouter } from 'expo-router';
import { GestureHandlerRootView } from 'react-native-gesture-handler';
import * as Notifications from 'expo-notifications';
import { useUserStore } from '../store/userStore';
import { registerDevicePushToken } from '../utils/notifications';

Notifications.setNotificationHandler({
  handleNotification: async () => ({
    shouldShowAlert: true,
    shouldPlaySound: true,
    shouldSetBadge: false,
  }),
});

export default function RootLayout() {
  const router = useRouter();
  const { token, restoreSession } = useUserStore();
  const [isRestoring, setIsRestoring] = useState<boolean>(true);
  const pushRegistrationAttemptRef = useRef<string | null>(null);

  useEffect(() => {
    void restoreSession().finally(() => {
      setIsRestoring(false);
    });
  }, []);

  useEffect(() => {
    if (!isRestoring && token === null) {
      router.replace('/login');
    }
  }, [token, isRestoring]);

  useEffect(() => {
    if (token === null) {
      pushRegistrationAttemptRef.current = null;
      return;
    }

    if (pushRegistrationAttemptRef.current === token) return;
    pushRegistrationAttemptRef.current = token;

    void (async () => {
      try {
        const registered = await registerDevicePushToken(token);
        if (!registered && pushRegistrationAttemptRef.current === token) {
          pushRegistrationAttemptRef.current = null;
        }
      } catch {
        if (pushRegistrationAttemptRef.current === token) {
          pushRegistrationAttemptRef.current = null;
        }
      }
    })();
  }, [token]);

  if (isRestoring) {
    return (
      <GestureHandlerRootView style={{ flex: 1 }}>
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
          <ActivityIndicator size="large" />
        </View>
      </GestureHandlerRootView>
    );
  }

  return (
    <GestureHandlerRootView style={{ flex: 1 }}>
      <Stack screenOptions={{ headerShown: false }}>
        <Stack.Screen name="(tabs)" options={{ headerShown: false }} />
        <Stack.Screen name="login" options={{ headerShown: false }} />
        <Stack.Screen name="register" options={{ headerShown: false }} />
      </Stack>
    </GestureHandlerRootView>
  );
}
```


TASK

Part A — Create src/utils/queryClient.ts (NEW FILE):
  Export a configured QueryClient instance named `queryClient`.
  Export an AsyncStorage persister named `asyncStoragePersister`.

  QueryClient configuration:
    - staleTime: 5 * 60 * 1000 (5 minutes)
    - gcTime: 24 * 60 * 60 * 1000 (24 hours) — use gcTime NOT cacheTime
    - retry: 3
    - retryDelay: exponential backoff — (attempt: number) => Math.min(1000 * 2 ** attempt, 30000)

  AsyncStoragePersister:
    - key: 'crm-query-cache'
    - storage: AsyncStorage (from @react-native-async-storage/async-storage)

Part B — Modify src/app/_layout.tsx:
  1. Import PersistQueryClientProvider from '@tanstack/react-query-persist-client'
  2. Import { queryClient, asyncStoragePersister } from '../utils/queryClient'
  3. Wrap both return paths (the "isRestoring" early return AND the main return) with PersistQueryClientProvider.
     - PersistQueryClientProvider must be OUTSIDE GestureHandlerRootView (as the outermost wrapper).
     - Props: client={queryClient} persistOptions={{ persister: asyncStoragePersister }}
  4. Keep ALL existing functionality unchanged (useUserStore, restoreSession, push registration, navigation effects, etc.)
  5. Keep GestureHandlerRootView as the direct child of PersistQueryClientProvider.


CONSTRAINTS

1. TypeScript only — zero `any`.
2. Do NOT remove or change any existing imports, hooks, effects, or logic in _layout.tsx.
3. Only ADD the PersistQueryClientProvider wrapper and its imports — no other changes.
4. src/utils/queryClient.ts must have no React imports — it's a plain TypeScript utility module.
5. gcTime not cacheTime (React Query v5 API).
6. Do NOT create a QueryClientProvider — PersistQueryClientProvider replaces it entirely.
7. No comments in either file.
8. Async storage key: 'crm-query-cache' (consistent cache key for the app).


OUTPUT

Two files:
1. src/utils/queryClient.ts (new)
2. src/app/_layout.tsx (modified — only the PersistQueryClientProvider wrapper added)


VALIDATION

After writing both files:
1. Run: cd /c/Users/fedor/crm && npx tsc --noEmit
2. Report any TypeScript errors.
3. Fix any errors before finishing.
