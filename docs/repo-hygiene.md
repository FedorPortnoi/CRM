# Repo Hygiene

Local run artifacts such as backend logs, `tmp-test-runs/`, and root-level
Codex handoff notes are ignored for new files. Existing tracked artifacts are
not deleted by these ignore rules; remove them from version control separately
when the team agrees they are no longer needed.
