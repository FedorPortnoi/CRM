CONTEXT

Project root: /c/Users/fedor/crm
React Native + Expo SDK 51 + TypeScript.
File to modify: src/app/(tabs)/kanban.tsx

The file has two view modes: board (KanbanBoard component) and list (DealListView component).
DealListView is an inner component (not exported) that renders a FlatList of deals.
DealListView uses useDealsStore and usePipelinesStore Zustand stores.
It calls fetchDeals() and fetchPipelines() from the stores.
The FlatList in DealListView has data={rows}.


TASK

Add pull-to-refresh to the DealListView FlatList in src/app/(tabs)/kanban.tsx.

Make exactly these 4 changes and nothing else:

1. Add RefreshControl to the react-native import.
   Current: import { View, Text, FlatList, TouchableOpacity, ActivityIndicator, StyleSheet } from 'react-native';
   New: import { View, Text, FlatList, TouchableOpacity, ActivityIndicator, StyleSheet, RefreshControl } from 'react-native';

2. Inside DealListView function body, add isRefreshing state after the existing store selectors (after the fetchPipelines line):
     const [isRefreshing, setIsRefreshing] = useState<boolean>(false);

   Also add the useState import — the file currently imports: import React, { useState, useEffect, useMemo } from 'react';
   useState is already imported, so no import change needed.

3. Add handleRefresh callback inside DealListView, after the existing useEffect:
     const handleRefresh = useCallback((): void => {
       setIsRefreshing(true);
       void Promise.all([fetchDeals(), fetchPipelines()]).finally(() => setIsRefreshing(false));
     }, [fetchDeals, fetchPipelines]);

   Also add useCallback to the React import:
   Current: import React, { useState, useEffect, useMemo } from 'react';
   New: import React, { useState, useEffect, useMemo, useCallback } from 'react';

4. Add refreshControl prop to the FlatList inside DealListView (the one with data={rows}):
     refreshControl={
       <RefreshControl
         refreshing={isRefreshing}
         onRefresh={handleRefresh}
         colors={['#1A73E8']}
         tintColor="#1A73E8"
       />
     }

Do NOT change anything else. All existing logic, styles, state, and JSX must remain identical.
Write the complete modified file to src/app/(tabs)/kanban.tsx.


CONSTRAINTS

1. TypeScript only — zero any.
2. No comments.
3. Keep ALL existing logic, styles, and JSX unchanged except the four changes listed above.
4. The finally in handleRefresh must call setIsRefreshing(false).
5. Only modify DealListView — do not change PipelineScreen or any other function.
6. The refreshControl goes on the FlatList<DealRow> (data={rows}), not any other element.


OUTPUT

Write the complete file: src/app/(tabs)/kanban.tsx


VALIDATION

After writing the file:
1. Run: npx tsc --noEmit
2. Report any TypeScript errors and fix all of them before finishing.
