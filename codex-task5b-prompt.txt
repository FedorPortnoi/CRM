CONTEXT

Project root: /c/Users/fedor/crm
React Native + Expo SDK 51 + TypeScript.
File to modify: src/app/(tabs)/tasks.tsx

The file currently has NO RefreshControl and NO isRefreshing state.
It uses a single FlatList (data={displayTasks}) as the main scrollable component.
fetchTasks() is the async data loading function (returns Promise<void>).


TASK

Add pull-to-refresh to src/app/(tabs)/tasks.tsx.

Make exactly these 4 changes and nothing else:

1. Add RefreshControl to the react-native import.
   Current: import { View, Text, FlatList, TouchableOpacity, StyleSheet, ListRenderItemInfo } from 'react-native';
   New: import { View, Text, FlatList, TouchableOpacity, StyleSheet, ListRenderItemInfo, RefreshControl } from 'react-native';

2. Add isRefreshing state after the existing isLoading/error state declarations:
     const [isRefreshing, setIsRefreshing] = useState<boolean>(false);

3. Add handleRefresh callback after handleRetry (around line 101) and before renderItem:
     const handleRefresh = useCallback((): void => {
       setIsRefreshing(true);
       void fetchTasks().finally(() => setIsRefreshing(false));
     }, [fetchTasks]);

4. Add refreshControl prop to the FlatList (the one with data={displayTasks}):
     refreshControl={
       <RefreshControl
         refreshing={isRefreshing}
         onRefresh={handleRefresh}
         colors={['#1A73E8']}
         tintColor="#1A73E8"
       />
     }

Do NOT change anything else. All existing logic, styles, state, and JSX must remain identical.
Write the complete modified file to src/app/(tabs)/tasks.tsx.


CONSTRAINTS

1. TypeScript only — zero any.
2. No comments.
3. Keep ALL existing logic, styles, and JSX unchanged except the four changes listed above.
4. The finally in handleRefresh must call setIsRefreshing(false).
5. handleRefresh does NOT reset isLoading to true (only isRefreshing).


OUTPUT

Write the complete file: src/app/(tabs)/tasks.tsx


VALIDATION

After writing the file:
1. Run: npx tsc --noEmit
2. Report any TypeScript errors and fix all of them before finishing.
