import { View, Text } from 'react-native';

export default function TasksScreen(): JSX.Element {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Tasks — coming in Sprint 5</Text>
    </View>
  );
}
