import { View, Text } from 'react-native';

export default function DashboardScreen(): JSX.Element {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Dashboard — coming in Sprint 5</Text>
    </View>
  );
}
